// import { fromJS } from 'immutable';
// import { makeSelectCauseDomain } from '../selectors';

// const selector = makeSelectCauseDomain();

describe('makeSelectCauseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
