// import { fromJS } from 'immutable';
// import { makeSelectMessagesDomain } from '../selectors';

// const selector = makeSelectMessagesDomain();

describe('makeSelectMessagesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
