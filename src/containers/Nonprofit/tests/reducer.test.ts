
// import nonprofitReducer from '../reducer';

describe('nonprofitReducer', () => {
  it('returns the initial state', () => {
    // expect(nonprofitReducer(undefined, {})).toEqual({});
    expect(true).toEqual(true);
  });
});
