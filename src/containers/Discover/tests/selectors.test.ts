// import { fromJS } from 'immutable';
// import { makeSelectDiscoverDomain } from '../selectors';

// const selector = makeSelectDiscoverDomain();

describe('makeSelectDiscoverDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
