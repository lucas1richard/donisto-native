// import { fromJS } from 'immutable';
// import { makeSelectOrganizationDomain } from '../selectors';

// const selector = makeSelectOrganizationDomain();

describe('makeSelectOrganizationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
