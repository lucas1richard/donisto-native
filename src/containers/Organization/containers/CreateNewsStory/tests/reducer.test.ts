
// import createNewsStoryReducer from '../reducer';

describe('createNewsStoryReducer', () => {
  it('returns the initial state', () => {
    // expect(createNewsStoryReducer(undefined, {})).toEqual({});
    expect(true).toEqual(true);
  });
});
