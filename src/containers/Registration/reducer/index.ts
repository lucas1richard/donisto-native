export const initialRegistrationState = {};

const registrationReducer = (state = initialRegistrationState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default registrationReducer;
