import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    marginTop: 20,
    borderRadius: 10,
    elevation: 3,
    shadowColor: '#555',
    shadowOpacity: 0.20,
    shadowOffset: {
      width: 2,
      height: 4
    },
    shadowRadius: 6,
    backgroundColor: '#fff'
  }
});

export default styles;
