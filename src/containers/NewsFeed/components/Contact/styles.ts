import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  outerwrapper: { alignItems: 'center', padding: 7.5 },
  wrapper: { height: 50, width: 50, borderRadius: 25, backgroundColor: '#f0f0f0' },
  namewrapper: { fontSize: 16, color: 'rgb(171,169,167)', marginTop: 4 }
});

export default styles;
